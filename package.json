{
  "name": "agnostic",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "clear": "rimraf --sync ./lib",
    "dev": "npm run clear && npm run generate-shared-dependencies && webpack -c ./webpack.modules.config.js --watch --mode=development",
    "build": "npm run clear && npm run generate-shared-dependencies && webpack -c ./webpack.modules.config.js --mode=production",
    "generate-shared-dependencies": "webpack -c ./webpack.shared.config.js --mode=production"
  },
  "keywords": [],
  "author": "Felippe Regazio",
  "license": "MIT",
  "devDependencies": {
    "glob": "^8.0.3",
    "nano-jsx": "^0.0.36",
    "rimraf": "^3.0.2",
    "ts-loader": "^9.4.1",
    "typescript": "^4.8.4",
    "webpack": "^5.74.0",
    "webpack-cli": "^4.10.0"
  }
}
