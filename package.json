{
  "name": "wcwc",
  "version": "1.0.0",
  "description": "Write Cool Web Components",
  "main": "lib/index.js",
  "types": "lib/d.ts",
  "files": [
    "lib/*"
  ],
  "scripts": {
    "clear": "rimraf --sync ./lib",
    "dev": "npm run build:dev -- --watch",
    "build": "npm run clear && webpack --mode=production",
    "build:dev": "npm run clear && webpack --mode=development",
    "test": "npm run build:dev && npm run test:module",
    "test:module": "node ./test/module/test.cjs && node ./test/module/test.mjs"
  },
  "keywords": [],
  "author": "Felippe Regazio",
  "license": "MIT",
  "devDependencies": {
    "rimraf": "^3.0.2",
    "terser-webpack-plugin": "^5.3.6",
    "ts-loader": "^9.4.1",
    "typescript": "^4.8.4",
    "webpack": "^5.74.0",
    "webpack-cli": "^4.10.0"
  }
}
